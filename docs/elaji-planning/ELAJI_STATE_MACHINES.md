# Elaji State Machines

**Status:** Placeholder — no planning content yet.

## Purpose

Elaji Health's own status/workflow state machines (e.g. appointment status, queue status, invoice status, encounter status) — informed by, and explicitly contrasted against, `docs/product-research/medpro-reference/extracted-blueprints/MEDPRO_WORKFLOWS.md` where relevant, but describing Elaji's actual or planned state machines, not MedPro's.

Where Elaji already has a given state machine implemented (e.g. `AppointmentStatus`, `QueueStatus`, `InvoiceStatus` in the Prisma schema and their transition rules in the relevant `.service.ts` files), this document should reference that as the current source of truth rather than duplicating it — this document is for planning new/changed state machines, and for explicitly noting where Elaji's design diverges from the MedPro reference and why.

## State machines

*(To be filled in as planning proceeds.)*

## Divergences from MedPro reference (explicit, with reasoning)

*(To be filled in.)*

## Open questions

*(To be filled in.)*
