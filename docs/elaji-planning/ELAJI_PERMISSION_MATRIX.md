# Elaji Permission Matrix

**Status:** Placeholder — no planning content yet.

## Purpose

Elaji Health's own role × permission matrix — informed by, and explicitly contrasted against, `docs/product-research/medpro-reference/extracted-blueprints/MEDPRO_PERMISSION_NOTES.md` where relevant, but describing Elaji's actual or planned permissions, not MedPro's.

Elaji's current permission matrix already exists at `docs/architecture/Permissions Matrix.md` (role × endpoint grants) and in code (`@Roles()` decorators across `apps/api/src/modules/**/*.controller.ts`, and `apps/web/src/lib/permissions.ts`). This document is for planning new/changed permissions arising from MedPro-informed roadmap work — not a duplicate of the existing matrix. Always check the existing matrix and actual code first; treat this document as additive, not authoritative over the real source of truth.

## Planned permission changes

*(To be filled in as planning proceeds.)*

## Divergences from MedPro reference (explicit, with reasoning)

*(To be filled in.)*

## Open questions

*(To be filled in.)*
